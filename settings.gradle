rootProject.name = 'maven-central-publishing-demo'

// The shared library we want published on maven central.
include 'simple-int-arithmetic-lib'

// An app which consumes the shared library which we publish on maven central.
include 'consumer-app'